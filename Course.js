function enrollCourse(courseName) {
    alert("You have enrolled in " + courseName);
    // Additional enrollment logic can be added here, such as updating the user's profile or sending a confirmation email.
}
